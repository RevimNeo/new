# A new version or Revim (maybe a new name)

Now cleaner, faster, and much readable plugins!.

Some plugins are remove such as whichkey, gitsigns, LazyGit and etc.

## Install
```bash
git clone https://github.com/RevimNeo/new ~/.config/nvim
```

### Stable
